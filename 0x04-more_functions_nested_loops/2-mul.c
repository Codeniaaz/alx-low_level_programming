#include "main.h"

/**
 * mul - used for multiplication of two integers
 * @a: is an integer
 * @b: is an integer
 * Return: always 0.
*/

int mul(int a, int b)
{
	int result;

	result = a * b;
	printf("%d * %d = %d", a, b, result);

	return (0);
}
